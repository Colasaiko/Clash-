# 节点质量判断篇

很多新手选择节点时，只看 Clash 显示的 `20ms`、`40ms` 或 `60ms`。

实际上，一个节点是否好用，通常需要综合考虑：

- 延迟
- 丢包
- 速度
- 稳定性
- 高峰期表现
- 本地网络
- 节点线路
- 目标网站的网络情况

因此更合理的判断方式是：

> **不要只寻找“最低延迟”，而应该寻找最适合自己使用场景的节点。**

---

## 1. 延迟（Latency / Ping）

延迟表示数据在网络中传输并得到响应所需要的时间，通常使用 `ms`（毫秒）表示。

简单理解：

```text
设备
 ↓
节点
 ↓
测试目标
 ↓
返回设备
```

如果响应需要 50ms，那么测试结果可能显示为约 `50ms`。

### 延迟参考

| 延迟 | 一般体验 | 常见用途 |
|---|---|---|
| 20～50ms | 较低 | 实时交互、网页 |
| 50～100ms | 较好 | 日常浏览、办公、视频 |
| 100～150ms | 可以接受 | 普通网页和部分在线服务 |
| 150～250ms | 较高 | 对延迟要求较低的场景 |
| 250ms 以上 | 明显偏高 | 不适合对实时性要求较高的场景 |

> 以上只是帮助理解的参考范围，不是绝对标准。实际体验还会受到线路、路由、应用服务器等因素影响。

### 为什么低延迟不一定代表真实体验最好？

Clash 的延迟测试通常针对指定的测试目标。

实际访问网站时，网络路径可能不同：

```text
你的设备
 ↓
Clash
 ↓
节点服务器
 ↓
目标网站
 ↓
返回
```

因此：

```text
节点 A：40ms
节点 B：60ms
```

并不意味着节点 A 访问所有网站都会比节点 B 快。

Mihomo 的代理组支持通过测试 URL 和健康检查来判断节点状态，因此不同测试目标得到的结果可能存在差异。

官方资料：

- [Mihomo Proxy Groups](https://wiki.metacubex.one/en/config/proxy-groups/)
- [Mihomo API](https://wiki.metacubex.one/en/api/)

---

## 2. 丢包（Packet Loss）

丢包可以简单理解为：

> **发送出去的数据包没有成功完成传输。**

例如：

```text
发送：100 个数据包
成功：99 个
丢失：1 个

丢包率：1%
```

丢包率越高，网络连接通常越容易出现异常。

### 可能出现的表现

- 网页偶尔打不开
- 视频突然缓冲
- 图片加载不完整
- 游戏出现瞬间卡顿
- 语音或视频通话断续
- 下载速度忽快忽慢
- 长连接偶尔断开

因此：

> **低延迟 + 高丢包，并不一定是好节点。**

例如：

```text
节点 A
延迟：35ms
丢包：5%

节点 B
延迟：65ms
丢包：0.5%
```

对于需要持续连接的应用来说，节点 B 可能反而更加稳定。

---

## 3. 速度（Speed / Bandwidth）

速度是另一个重要指标，但需要注意：

> **测速结果并不等于所有网站、所有时间段都能达到相同速度。**

实际速度会受到很多因素影响：

- 本地宽带
- Wi-Fi 信号
- 节点带宽
- 节点负载
- 网络高峰
- 目标网站服务器
- 网络路由
- 下载方式

例如本地宽带本身最大速度为 `100 Mbps`，即使节点服务器带宽很高，最终使用体验仍然会受到本地网络能力限制。

### 建议测试方法

可以观察：

```text
开始速度
平均速度
峰值速度
速度波动
```

同时最好进行多次测试，而不是只测试一次。

---

## 4. 稳定性（Stability）

稳定性是长期使用时非常重要的指标。

例如：

```text
节点 A

第一次：45ms
第二次：47ms
第三次：44ms
第四次：46ms
```

另一个节点：

```text
节点 B

第一次：25ms
第二次：180ms
第三次：40ms
第四次：320ms
```

虽然节点 B 曾经达到 `25ms`，但波动非常明显。

因此：

> **稳定的 50ms，通常比忽高忽低的 20ms 更值得长期使用。**

---

## 5. 高峰期表现

一个节点白天表现很好，并不意味着晚上也一定保持相同表现。

如果节点在某些时间段负载增加，可能出现：

```text
延迟上升
 ↓
丢包增加
 ↓
速度下降
 ↓
网页加载变慢
 ↓
连接稳定性下降
```

如果你主要在晚上使用，那么建议在自己的实际使用时间测试节点。

例如：

| 时间 | 节点 A | 节点 B | 节点 C |
|---|---:|---:|---:|
| 10:00 | 45ms | 60ms | 52ms |
| 15:00 | 48ms | 63ms | 55ms |
| 20:00 | 51ms | 130ms | 70ms |
| 22:00 | 55ms | 180ms | 73ms |

从这个例子可以看到：

节点 B 白天表现不错，但高峰期延迟明显增加。

如果主要在晚上使用，那么节点 C 可能更加适合。

---

**学习导航**
- [👈 上一篇：Clash 基础配置](./01-Clash基础配置.md)
- [🏠 返回教程首页](./)
- [👉 下一篇：实际测试节点](./03-实际测试节点.md)
