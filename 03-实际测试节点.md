# 实际测试节点

了解指标之后，可以建立一套简单的测试流程。

## 第一步：测试 Clash 延迟

打开 Clash Verge Rev 的代理页面，对节点进行延迟测试。

例如：

```text
香港 01    45ms
香港 02    52ms
日本 01    68ms
新加坡 01  75ms
美国 01    160ms
```

第一轮可以排除明显无法连接或延迟异常高的节点。

但不要马上认为：

```text
45ms = 最佳节点
```

还需要进行后续测试。

---

## 第二步：测试实际网页访问

可以选择不同类型的网站进行测试，例如：

- 普通网页
- 图片资源
- 视频服务
- 代码托管平台
- 在线工具
- 云服务

因为不同网站可能经过不同的网络路径。

一个节点访问某个网站很快，不代表访问所有网站都会一样。

---

## 第三步：检查公网 IP

如果需要查看当前连接使用的公网 IP，可以使用：

**[IPinfo](https://ipinfo.io/)**

它可以用于辅助查看公网 IP 和网络相关信息。

例如可以帮助排查：

- 更换节点后 IP 是否发生变化
- 当前出口 IP 的网络信息
- 不同节点之间的 IP 差异

> IP 数据库并非实时且绝对准确，不同服务显示的信息可能存在差异。

---

## 第四步：进行速度测试

可以使用：

**[Cloudflare Speed Test](https://speed.cloudflare.com/)**

测试时不要只关注下载速度。

可以综合观察：

```text
Download
Upload
Latency
网络波动
```

建议在不同时间段进行测试。

---

## 第五步：连续观察

如果希望长期使用某个节点，可以连续观察一段时间：

```text
第一次测试
 ↓
第二次测试
 ↓
高峰期测试
 ↓
实际使用
 ↓
再次测试
```

这样比一次测试得出的结果更加可靠。

---

# Clash 自动选择策略

如果配置文件中存在：

```text
自动选择
Auto
URL-Test
Fallback
Load-Balance
```

等策略组，可以减少频繁手动切换节点的需要。

Mihomo 官方文档提供了多种代理组策略，包括 `select`、`url-test`、`fallback` 和 `load-balance`。

官方文档：

https://wiki.metacubex.one/en/config/proxy-groups/

---

## 1. Select：手动选择

`select` 是最容易理解的方式。

例如：

```text
节点选择
├── 香港 01
├── 香港 02
├── 日本 01
├── 新加坡 01
└── 美国 01
```

用户自己选择一个节点。

### 优点

- 简单
- 可控
- 方便固定使用某个节点

### 缺点

- 节点出现问题时需要手动切换

官方资料：

https://wiki.metacubex.one/en/config/proxy-groups/select/

---

## 2. URL-Test：自动测试

URL-Test 可以理解为：

> **定期测试节点，并根据测试结果进行自动选择。**

例如：

```text
香港 01 → 45ms
香港 02 → 51ms
日本 01 → 70ms
新加坡 01 → 80ms
```

可以根据测试策略自动选择当前表现较好的节点。

官方资料：

https://wiki.metacubex.one/en/config/proxy-groups/url-test/

---

## 3. Fallback：故障切换

Fallback 更关注“当前节点是否可用”。

简单理解：

```text
主节点
 ↓
无法连接
 ↓
备用节点
 ↓
继续连接
```

例如：

```text
香港 01
 ↓
不可用
 ↓
香港 02
 ↓
不可用
 ↓
日本 01
```

这样可以减少某个节点临时不可用时需要手动处理的情况。

官方资料：

https://wiki.metacubex.one/en/config/proxy-groups/fallback/

---

## 4. Load-Balance：负载均衡

`load-balance` 更适合已经了解代理组和规则配置的进阶用户。

它可以根据不同策略在多个节点之间进行流量分配。

常见策略包括：

- `round-robin`
- `consistent-hashing`

普通新手暂时不需要深入研究。

先掌握：

```text
Rule
 ↓
Select
 ↓
URL-Test
 ↓
Fallback
```

已经可以满足大部分日常使用需求。

官方资料：

https://wiki.metacubex.one/en/config/proxy-groups/load-balance/

---

**学习导航**
- [👈 上一篇：节点质量判断](./02-节点质量判断.md)
- [🏠 返回教程首页](./)
- [👉 下一篇：新手常见误区](./04-新手常见误区.md)
